import React from 'react';
import { Button } from 'reactstrap';

const Bookreturn = (props) => {
  return (
    <Button color="secondary" size="lg" block>반납</Button>
  );
}

export default Bookreturn;
