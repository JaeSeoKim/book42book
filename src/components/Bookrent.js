import React from 'react';
import { Container, Button } from 'reactstrap';

const Bookrent = (props) => {
  return (
	<Container>
    	<Button color="secondary" size="lg" block>대출</Button>
	</Container>
  );
}

export default Bookrent;
